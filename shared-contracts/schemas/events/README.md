# Event Schemas

Place versioned event envelope and payload schemas here.

Focus areas:

- auditability metadata (`tenant_id`, `org_id`, `correlation_id`)
- stable event type names
- explicit payload versioning
