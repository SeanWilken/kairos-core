# v1 Routes

Route modules for API version 1.

Current route set is intentionally small while the contract baseline is established.

- `health.py` provides a simple runtime health check using the shared response envelope.
