# v1 Contract Tests

These tests define and lock the v1 API behavior.

Current focus:

- `GET /v1/health`
- success envelope shape
- error envelope shape for missing routes

When v2 is introduced, v1 tests should remain green unless v1 is explicitly deprecated.
