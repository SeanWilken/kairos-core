# Shared Contract Invariants

Tests in this folder apply to all versions.

Examples:

- required metadata fields
- stable error code mapping
- envelope baseline expectations

If a future version intentionally breaks one of these, update this folder and document why in an ADR.
