# Core Unit Tests

This folder tests reusable core helpers:

- metadata builders
- response envelope helpers
- error code mapping

These tests are useful when refactoring response contracts or metadata policies.
